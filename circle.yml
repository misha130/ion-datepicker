dependencies:
  pre:
    - npm install -g karma-cli bower codeclimate-test-reporter
    - bower install
  cache_directories:
    - ~/nvm

test:
  override:
    - npm test
    - CODECLIMATE_REPO_TPKEN=$CodeClimateToken codeclimate-test-reporter < ./coverage/**/lcov.info
